{
  "name": "paidar-ai",
  "version": "1.0.0",
  "description": "Paidar.ai website for Cloudflare Pages",
  "scripts": {
    "dev": "wrangler pages dev site --compatibility-date=2024-04-01",
    "build": "echo 'Static assets and functions ready for deployment' && exit 0"
  },
  "devDependencies": {
    "wrangler": "^4.83.0"
  }
}
